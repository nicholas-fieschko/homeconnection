%h1 Listing reviews

%table
  %thead
    %tr
      %th Public comments
      %th Private comments
      %th Rating
      %th
      %th
      %th

  %tbody
    - @reviews.each do |review|
      %tr
        %td= review.public_comments
        %td= review.private_comments
        %td= review.rating
        %td= link_to 'Show', user_review_path(id: review.id, user_id: params[:user_id])
        %td= link_to 'Edit', edit_user_review_path(id: review.id, user_id: params[:user_id])
        %td= link_to 'Destroy', user_review_path(id: review.id, user_id: params[:user_id]), :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Review', new_user_review_path
