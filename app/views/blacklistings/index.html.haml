%h1 Listing blacklistings

%table
  %thead
    %tr
      %th User
      %th Blocked user
      %th
      %th
      %th

  %tbody
    - @blacklistings.each do |blacklisting|
      %tr
        %td= blacklisting.user_id
        %td= blacklisting.blocked_user
        %td= link_to 'Show', blacklisting
        %td= link_to 'Edit', edit_blacklisting_path(blacklisting)
        %td= link_to 'Destroy', blacklisting, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Blacklisting', new_blacklisting_path
